PORT=4000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fiber_network

# Used only in production (knexfile.js production block)
# DATABASE_URL=postgres://user:pass@host:5432/fiber_network
